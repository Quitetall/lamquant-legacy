# lamquant-legacy

Optional, process-isolated adapters for retired LamQuant wire formats. This
repository is not a library dependency of ABIR, BLUT, firmware, training, or
the main LamQuant runtime.

The initial adapter provides bounded inspection and exact, non-destructive
forensic conversion. Semantic conversion capabilities are advertised only
after their format-specific mapping tests pass.
